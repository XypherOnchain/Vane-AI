{
  "name": "@vane/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "migrate": "echo 'No migration runner yet (lands in PR2). Bootstrap schema infrastructure/sql/001_init.sql is applied by docker compose on first start.' && exit 0"
  }
}
