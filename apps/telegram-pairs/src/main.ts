console.log("Vane New Pairs bot stub — webhooks validated via API /internal/telegram/pairs/webhook");
console.log("Full bot deferred until web intelligence is stable (per build plan).");
setInterval(() => {}, 60_000);
